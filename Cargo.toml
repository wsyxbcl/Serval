[package]
name = "serval"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "4.4.7", features = ["derive"] }
polars = { version = "0.34.2", features = ["lazy", "strings", "list_eval", "csv", "dtype-struct"] }
xmp_toolkit = "1.6.0"

